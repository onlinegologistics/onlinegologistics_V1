services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: onlinegologistics_backend
    restart: always
    ports:
      - "5003:5003"
    env_file:
      - ./backend/.env
    volumes:
      - ./wa-auth:/app/wa-auth
    networks:
      - onlinegologistics_network

networks:
  onlinegologistics_network:
    driver: bridge
